[sqlfluff]
# Use jinja templater so SQLFluff doesn't need a DB connection
templater = jinja
dialect = snowflake
max_line_length = 120

[sqlfluff:rules:capitalisation.keywords]
# Keep SQL keywords lowercase (select, from, etc.)
capitalisation_policy = lower
